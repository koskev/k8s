---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: grocy-storage
spec:
  # Add 'kadalu.' to name from KadaluStorage kind
  storageClassName: kadalu.ssd-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
